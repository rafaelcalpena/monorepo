// add flow later

import indexify from '../src/index'

describe('Indexify', () => {
  it('is defined', () => {
    expect(indexify).toBeDefined();
  })

  it('accepts array as input', () => {
    
  })

});
